# @stateguard/shared

Shared contracts and constants used across the StateGuard monorepo.

Keep the API request/response shapes here in sync with the backend Pydantic
models in `services/api/app/models/schemas.py` so the frontend and backend
agree on the wire format.
