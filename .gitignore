# OS / editor cruft
Thumbs.db
.DS_Store
*.tmp
*~

# Secrets — never commit (the bot reads keys from env vars / secret store, not the repo)
.env
.env.*
*.key
*.pem
secrets.*
credentials.*

# Local-only
.claude/
node_modules/
